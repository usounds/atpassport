{
  "name": "atpassport",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "update": "pnpm -r update"
  },
  "devDependencies": {
    "npm-check-updates": "^17.1.18",
    "sst": "^4.5.11",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "19.2.14",
      "@types/react-dom": "19.2.3",
      "esbuild": "0.25.0"
    }
  }
}
