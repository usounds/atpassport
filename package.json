{
  "name": "atpassport",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "sst": "^4.5.1",
    "typescript": "^5.0.0"
  }
}
