# Changesets

Hello! This directory stores the changesets for the project.
A changeset is a small file that describes a change and which packages it should affect.
When you are ready to release, run `pnpm changeset` to create a new changeset.
