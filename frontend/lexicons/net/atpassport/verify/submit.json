{
  "lexicon": 1,
  "id": "net.atpassport.verify.submit",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Submit a domain verification request via OAuth.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["isPublic"],
          "properties": {
            "isPublic": { "type": "boolean" }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["success"],
          "properties": {
            "success": { "type": "boolean" },
            "error": { "type": "string" }
          }
        }
      }
    }
  }
}
