# @passport への貢献ガイドライン

@passport に関心を持っていただきありがとうございます！あらゆる形での貢献を歓迎します。

## 貢献方法

### バグ報告

バグを見つけた場合は、GitHub の Issue を作成し、以下の内容を含めてください。
- 明確で説明的なタイトル
- 再現手順
- 期待される動作と実際の動作
- 可能であればスクリーンショットやログ

### 機能提案

新しいアイデアを歓迎します！機能を提案する場合は、GitHub の Issue を作成し、どのような機能が必要か、なぜそれが必要かについて記載してください。

### コードでの貢献

1. **リポジトリをフォーク**し、`main` からブランチを作成します。
2. **開発環境をセットアップ**します。
   - [Node.js](https://nodejs.org/) と [pnpm](https://pnpm.io/) がインストールされていることを確認してください。
   - ルートディレクトリで `pnpm install` を実行します。
3. **変更を加えます**。
4. **テストとリンターを実行**します。
   - `pnpm test` を実行して、すべてのテストがパスすることを確認します。
   - `pnpm lint` を実行して、コードスタイルの問題がないか確認します。
5. **プルリクエスト (PR) を送信**します。
   - **プルリクエストは `preview` ブランチに対して送信してください**。
   - 変更内容に関する明確な説明を記載してください。
   - 関連する Issue があればリンクしてください。

### 翻訳の追加 (i18n)

@passport は多言語に対応しています。新しい翻訳を追加したり、既存の翻訳を改善したりするには、以下の手順に従ってください。

> **注意**: 日本語以外のすべての言語は、現在機械翻訳によって生成されています。より自然な表現への改善を心より歓迎します。

1. **メッセージファイル**（`packages/frontend/messages/` 内）を確認します。
2. **言語に対応する JSON ファイル**を追加または編集します（例：フランス語の場合は `fr.json`）。
3. `packages/frontend/src/i18n/routing.ts` の `locales` 配列に、新しい言語コードを**登録**します。
4. **プルリクエストを送信**します。

## ライセンス

本プロジェクトに貢献することで、あなたの貢献内容が MIT ライセンスの下で提供されることに同意したものとみなされます。
