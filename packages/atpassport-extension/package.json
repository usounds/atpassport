{
  "name": "atpassport-extension",
  "version": "0.1.3",
  "description": "at://passport handle auto-filler and clipboard copier.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:chrome": "BROWSER=chrome vite build && cd dist && zip -r ../atpassport-extension-chrome.zip .",
    "build:firefox": "BROWSER=firefox vite build && cd dist && zip -r ../atpassport-extension-firefox.zip .",
    "build": "pnpm run build:chrome && pnpm run build:firefox",
    "package:source": "zip -r atpassport-extension-source.zip . -x \"node_modules/*\" \"dist/*\" \".git/*\" \"*.zip\" \".pnpm-store/*\" \".DS_Store\"",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "update": "npx ncu -u && pnpm install",
    "deploy:firefox": "pnpm run build:firefox && source .env && web-ext sign --api-key $FIREFOX_ISSUER --api-secret $FIREFOX_SECRET --source-dir ./dist --artifacts-dir ./web-ext-artifacts --channel listed",
    "deploy:chrome": "pnpm run build:chrome && source .env && chrome-webstore-upload upload --source atpassport-extension-chrome.zip --extension-id $CHROME_EXTENSION_ID --client-id $CHROME_CLIENT_ID --client-secret $CHROME_CLIENT_SECRET --refresh-token $CHROME_REFRESH_TOKEN --auto-publish",
    "dev:firefox": "BROWSER=firefox vite build --watch & web-ext run --source-dir ./dist",
    "dev:chrome": "BROWSER=chrome vite build --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "lucide-react": "^0.344.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/chrome": "^0.0.262",
    "@types/node": "^25.5.0",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "^4.3.4",
    "chrome-webstore-upload-cli": "^3.5.0",
    "jsdom": "^29.0.1",
    "node-forge": "1.4.0",
    "picomatch": "4.0.4",
    "typescript": "^5.9.3",
    "vite": "^5.4.21",
    "vite-plugin-web-extension": "^4.5.0",
    "vitest": "^2.1.9",
    "web-ext": "^10.0.0"
  }
}